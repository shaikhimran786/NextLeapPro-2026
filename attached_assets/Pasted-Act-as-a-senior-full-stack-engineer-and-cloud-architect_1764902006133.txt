Act as a senior full-stack engineer and cloud architect working on my product Next Leap Pro (nextleappro.com), a community-driven learning and earning platform.

You need to refactor and enhance how we handle static assets and images across the website and admin panel with smart and logical controls around ownership and permissions.

Core requirement: use a cloud Storage bucket named "assets"

Configure the project to use a cloud application Storage bucket named "assets" for:

All website static assets that are suitable for CDN-style delivery.

All user-uploaded images (profiles, communities, chapters, events, etc.).

Implement a clean, centralized abstraction for:

Uploading files to the "assets" bucket.

Generating public or signed URLs for read access (as per our security and caching needs).

Deleting or updating assets when required.

Prefer a clean service/module, for example:

services/storage.ts or equivalent

uploadAsset(file, { folder, ownerId, entityType, entityId })

getAssetUrl(key)

deleteAsset(key, ownerId, entityType, entityId) with authorization checks

Use the existing stack patterns (e.g. Next.js API routes, server actions, ORM) and integrate cleanly.

Replace image path inputs with upload features

Currently we are using static image paths or manual URLs. We want proper image upload everywhere.

For all relevant website and admin forms, replace or augment the current image path/text fields with:

An image upload component that:

Lets the user select a file from local device.

Uploads it to the "assets" bucket.

Stores only the asset key / URL in the database.

Keep an optional field for manual URL only if it makes sense, but default flow should be upload.

Apply this to at least:

Creator profile image / cover image.

Community logo, banner, and any other visual assets.

Chapter images.

Event images (cover, thumbnail etc.).

Ensure the upload UI:

Shows a preview of the uploaded image.

Handles loading states and errors clearly.

Validates allowed file types, size limits, and dimensions as needed.

Prevents duplicate uploads where possible, or handles them consistently.

Ownership and permissions on uploaded images

Implement logical ownership control for assets:

For each uploaded asset, persist metadata that ties it to:

ownerId (user or creator who uploaded it).

entityType (e.g. "creator_profile", "community", "chapter", "event").

entityId (id of the specific record).

Permission rules:

A creator/user can:

Upload, update, and delete images only for records they own (their own profile, their communities, their chapters, their events).

A user must not be able to modify or delete images belonging to other users.

Admins may have elevated rights (based on existing RBAC rules), but still follow a clear and traceable authorization logic.

Enforce these rules at the API / server level, not just in the UI:

Before performing upload, update, or delete, check:

The authenticated user identity.

Their role.

Whether they are the owner or have admin authority over that entity.

When a community, chapter, event, or creator profile is deleted:

Consider what should happen to associated images.

At minimum, avoid dangling references in DB.

If you implement physical delete from the "assets" bucket, do it in a safe and consistent manner.

Access patterns and URL handling

Make sure URLs generated from the "assets" bucket:

Are safe to expose in the frontend.

Are cached appropriately by the browser/edge where beneficial.

All components that display images for:

Creator profile

Communities

Chapters

Events
must now use the URLs generated by the storage service instead of hard-coded local paths.

Ensure fallback behaviour:

If an image is missing, corrupted, or not yet uploaded, show a sensible placeholder image from the "assets" bucket.

Admin and website parity

The upload and display logic should work from:

Public/creator-facing website (where allowed).

Admin panel.

Admin views should:

Display the uploaded images for each entity.

Allow admin to update or remove images, subject to role-based rules.

Keep Admin and website data in sync:

Changes to images in Admin should reflect on the website quickly.

Fix any cache or revalidation issues so that a new upload or change appears without long delays.

Performance and robustness

Optimize upload and retrieval:

Use efficient client-side uploads (for example, direct upload to storage if supported, with secure tokens).

Avoid blocking the UI while uploads are in progress. Provide progress and clear feedback.

Add server-side validation and sanitization:

Check file type, size, and basic safety before storing.

Make sure there are no orphaned or unreachable assets after updates and deletes.

Log errors and handle edge cases gracefully.

Implementation steps

Inspect current image handling in:

Creator profile

Community

Chapter

Event

Admin sections

Introduce the "assets" storage service/module.

Wire up upload endpoints / server actions with proper auth and ownership checks.

Update all relevant forms and components to use the new upload flow.

Replace display paths with URLs from the "assets" bucket.

Test end-to-end:

Creator uploads and updates their own images.

Creator cannot modify others’ assets.

Admin behaviour and overrides.

Deleted entities and their associated images.

No broken images or 404s.

Reasonable performance and responsiveness.

Start by scanning the codebase for existing image usage and storage logic, then design the storage service and ownership model, and finally implement the upload and CRUD flows step by step.